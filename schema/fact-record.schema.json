{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mark-brannan/colregs/schema/fact-record.schema.json",
  "title": "FactRecord",
  "description": "What a consumer asserts about one vessel at one moment: the input evaluateDisplay reads (ADR 0011 §2, ADR 0014). Structure only -- which fact keys exist and which values each takes is data/facts.json's, checked in the tests; see docs/adr/0006-json-schema-and-identifier-diff.md.",
  "$comment": "The same shape as a case's `facts` in schema/applicability-fixtures.schema.json, stated once here so an operation can name it.",
  "type": "object",
  "additionalProperties": false,
  "patternProperties": {
    "^fact:[a-z0-9_]+$": {
      "$ref": "#/$defs/factValue"
    }
  },
  "$defs": {
    "factValue": {
      "oneOf": [
        { "type": "string", "pattern": "^[a-z][a-z0-9_]*:[a-z0-9_]+$" },
        { "type": "number" },
        { "type": "boolean" }
      ]
    }
  }
}
