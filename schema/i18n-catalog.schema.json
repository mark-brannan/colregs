{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mark-brannan/colregs/schema/i18n-catalog.schema.json",
  "title": "data/i18n/*.json",
  "description": "Display catalog for one BCP 47 language: UI strings for the closed vocabularies the package emits (lights, shapes, modalities, roles, encounters, jurisdictions, closed-list fact values), not legal text -- see docs/adr/0003-language-as-a-dimension.md 'Display catalogs' and REQ-LANG-6.",
  "type": "object",
  "additionalProperties": false,
  "required": ["language", "provenance", "strings"],
  "properties": {
    "language": {
      "type": "string",
      "pattern": "^[a-z]{2,3}(-[A-Za-z0-9]+)*$",
      "description": "BCP 47 tag, e.g. en, fi. Must equal the filename stem; test/data.test.mjs checks."
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["contributors", "reviewed_by", "review_date"],
      "properties": {
        "contributors": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
        "reviewed_by": { "type": "array", "items": { "type": "string" }, "description": "Named human reviewers. Empty means unreviewed: the strings are on file but not yet vouched for." },
        "review_date": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
        "licence": { "type": "string", "description": "Optional until the data licence question (Q-9) is ruled; absent means the package licence applies." }
      }
    },
    "strings": {
      "type": "object",
      "description": "One section per vocabulary (REQ-LANG-6, REQ-LANG-9's ruling docs/adr/0017). Every section is keyed by the identifier itself, as documented in docs/identifiers.md -- light, modality, role and encounter identifiers carry a type prefix; jurisdiction and closed-list fact values are keyed by their own bare or fact-prefixed form. Values are static strings: no interpolation, no plurals -- the consumer's i18n system owns composition.",
      "additionalProperties": false,
      "properties": {
        "lights": {
          "type": "object",
          "propertyNames": { "pattern": "^light:[a-z_]+$" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        },
        "shapes": {
          "type": "object",
          "propertyNames": { "pattern": "^shape:[a-z_]+$" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        },
        "modalities": {
          "type": "object",
          "propertyNames": { "pattern": "^modality:[a-z]+(-[a-z]+)*$" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        },
        "roles": {
          "type": "object",
          "propertyNames": { "pattern": "^role:[a-z]+(-[a-z]+)*$" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        },
        "encounters": {
          "type": "object",
          "propertyNames": { "pattern": "^encounter:[a-z]+(-[a-z]+)*$" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        },
        "jurisdictions": {
          "type": "object",
          "propertyNames": { "pattern": "^[a-z]+(/[a-z]+)*$" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        },
        "facts": {
          "type": "object",
          "description": "Closed-list fact-value labels only (Tier A): propulsion, activity, position, rule18_class, dynamics, obstruction_side, wind_side. Field names (fact:length_m, geo:tcpa_s, ...) have no closed list behind them and are never catalogued here -- form-label text and unit/number formatting are the consumer's.",
          "propertyNames": { "pattern": "^[a-z][a-z0-9_]*:[a-z][a-z0-9_]*$" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        }
      }
    }
  },
  "$defs": {
    "label": { "type": "string", "minLength": 1 }
  }
}
