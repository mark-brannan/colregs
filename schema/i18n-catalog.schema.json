{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mark-brannan/colregs/schema/i18n-catalog.schema.json",
  "title": "data/i18n/*.json",
  "description": "Display catalog for one BCP 47 language: UI strings for identifier vocabularies (light names, fact-axis labels, modality labels, image captions), not legal text -- see docs/adr/0003-language-as-a-dimension.md 'Display catalogs'.",
  "type": "object",
  "additionalProperties": false,
  "required": ["language", "provenance", "strings"],
  "properties": {
    "language": {
      "type": "string",
      "pattern": "^[a-z]{2,3}(-[A-Za-z0-9]+)*$",
      "description": "BCP 47 tag, e.g. en, fi. Must equal the filename stem; test/data.test.mjs checks."
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["contributors", "reviewed_by", "review_date"],
      "properties": {
        "contributors": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
        "reviewed_by": { "type": "array", "items": { "type": "string" }, "description": "Named human reviewers. Empty means unreviewed: the strings are on file but not yet vouched for." },
        "review_date": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
        "licence": { "type": "string", "description": "Optional until the data licence question (Q-9) is ruled; absent means the package licence applies." }
      }
    },
    "strings": {
      "type": "object",
      "description": "One section per vocabulary. Every section is keyed by the identifier itself, as documented in docs/identifiers.md -- light identifiers (light:) and closed-vocabulary identifiers (modality:, and any of role:/encounter:/category: that gain a catalog section) alike, as stored in data/applicability.json. Values are static strings: no interpolation, no plurals -- the consumer's i18n system owns composition.",
      "additionalProperties": false,
      "properties": {
        "light": {
          "type": "object",
          "propertyNames": { "pattern": "^light:[a-z_]+$" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        },
        "modality": {
          "type": "object",
          "propertyNames": { "pattern": "^modality:[a-z]+(-[a-z]+)*$" },
          "additionalProperties": { "$ref": "#/$defs/label" }
        }
      }
    }
  },
  "$defs": {
    "label": { "type": "string", "minLength": 1 }
  }
}
